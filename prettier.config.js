module.exports = {
    tabWidth: 4, // 缩进级别的空间宽度（默认值：2）
    useTabs: false, // 使用制表符代替缩进空格（默认值：false）
    printWidth: 120, // 使用不同的最大行长度（默认值：80）
    usePrettier: true, // 对嵌入内容使用更漂亮（默认值：true）
    // "prettier": 为嵌入内容使用自定义更漂亮的设置（默认值：本地配置）
    singleQuote: false, // 使用单引号代替双引号（默认值：false）
    wrapAttributes: false, // 强制换行属性（当它有多个时，默认值为false）
    sortAttributes: true // 按字母顺序排序属性（默认值：false）
};
